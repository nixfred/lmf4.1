{
  "name": "lmf4-mem-cli",
  "version": "4.1.0",
  "description": "LMF4 memory CLI — SQLite + FTS5 + optional semantic search for Claude Code persistent memory",
  "type": "module",
  "bin": {
    "mem": "./dist/index.js"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node",
    "dev": "bun run src/index.ts"
  },
  "dependencies": {
    "commander": "^12.1.0"
  }
}
